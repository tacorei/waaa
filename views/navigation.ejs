<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="/top">AkiApp</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item"><a class="nav-link" href="/tasks">タスク</a></li>
                <li class="nav-item"><a class="nav-link" href="/friends">フレンド</a></li>
                <li class="nav-item"><a class="nav-link" href="/profile">プロフィール</a></li>
                <li class="nav-item"><a class="nav-link" href="/top">トップ</a></li>
            </ul>

            <!-- ✅ `user` が undefined でもエラーにならないように修正 -->
            <% if (user) { %>
                <div class="d-flex align-items-center">
                    <img src="<%= user.profile_image || 'https://via.placeholder.com/50' %>" class="rounded-circle me-2" width="40" height="40">
                    <span class="me-3"><%= user.username %></span>
                    <a href="/logout" class="btn btn-outline-danger btn-sm">ログアウト</a>
                </div>
            <% } else { %>
                <a href="/login" class="btn btn-primary btn-sm">ログイン</a>
            <% } %>
        </div>
    </div>
</nav>

<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>プロフィール編集</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5">
        <h1 class="text-center mb-4">プロフィール編集</h1>

        <% if (message.length > 0) { %>
            <div class="alert alert-success"><%= message %></div>
        <% } %>

        <div class="text-center mb-3">
            <img src="<%= user.profile_image || 'https://via.placeholder.com/150' %>" class="rounded-circle" width="150" height="150">
        </div>

        <form action="/update-profile" method="POST" class="card p-4">
            <div class="mb-3">
                <label class="form-label">ユーザー名</label>
                <input type="text" name="username" class="form-control" value="<%= user.username %>" required>
            </div>
            <div class="mb-3">
                <label class="form-label">プロフィール画像URL</label>
                <input type="text" name="profile_image" class="form-control" value="<%= user.profile_image || '' %>">
            </div>
            <button type="submit" class="btn btn-primary w-100">更新</button>
        </form>
    </div>
</body>
</html>
